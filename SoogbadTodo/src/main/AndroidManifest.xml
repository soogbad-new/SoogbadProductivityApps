<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".SoogbadTodoApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:hasFragileUserData="true">

        <activity
            android:name=".TodoListListActivity"
            android:label="SoogbadTodo"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TodoListActivity" android:exported="true" />
        <activity android:name=".EditDefaultTextActivity" android:exported="false" />
        <activity android:name="com.soogbad.sharedmodule.ui.RecycleBinActivity" android:exported="false" />

        <receiver android:name=".TodoAlarmReceiver" android:exported="false" />

        <receiver android:name="com.soogbad.sharedmodule.scheduling.ItemBootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>